1. Mustache语法（双大括号语法）：支持变量和简单表达式
2. 指令的使用（插值语法）
    v-once：重新响应（赋值）后，界面不随数据发生改变
    v-html：以html文件形式显示，例如显示带地址的标签，而不是源代码
    v-text：直接显示文本     <h2 v-text="message"></h2>   （一般不用，覆盖原来文本）
    v-pre：将大括号中的内容原封不动显示出来，不做任何解析
    v-cloak：给div赋予一个属性，当div中内容被解析时，属性自动消失（在网络延迟时，不显示代码页面） 